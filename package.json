{
  "name": "transportes-live",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": ">=12.6.0",
    "yarn": "^1.17.3"
  },
  "scripts": {
    "client:build": "cd ./client && yarn install --production=false && yarn prepare && yarn build",
    "worker": "cd ./worker && yarn start",
    "fix": "prettier --write \"**/*.js\"",
    "lint": "eslint \"**/*.js\" && prettier --check \"**/*.js\"",
    "heroku-postbuild": "cd ./worker && yarn install"
  },
  "dependencies": {
    "transportes": "^0.0.11"
  },
  "devDependencies": {
    "eslint": "^7.2.0",
    "prettier": "^2.0.5"
  }
}
