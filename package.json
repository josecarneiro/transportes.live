{
  "name": "transportes-live",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": ">=12.6.0",
    "yarn": "^1.17.3"
  },
  "scripts": {
    "worker": "cd ./worker && npm start",
    "client:build": "cd ./client && yarn install --production=false && yarn build",
    "postinstall": "cd ./worker && yarn i"
  }
}
